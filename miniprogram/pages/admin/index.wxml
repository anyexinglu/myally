<view class="container">
  <view class="card">
    <text class="title">🧪 调试模式</text>
    <text class="desc">开启后，你的真实画像和记忆不受任何影响，所有操作只存在本地。关闭调试后自动恢复。</text>

    <view class="switch-row">
      <text class="switch-label">调试模式</text>
      <switch checked="{{debugOn}}" bindchange="toggleDebug" color="#07c160"/>
    </view>

    <view wx:if="{{debugOn}}" class="debug-actions">
      <button class="btn" bindtap="startOnboarding">🔄 重置为新人体验</button>
      <text class="hint">点此按钮后，下次进入小程序会弹出新人问卷。填完问卷后可在精选页预览各 tab 的效果。关闭调试后一切恢复。</text>
    </view>
  </view>
</view>
