## 1. OpenSpec与测试基线

- [x] 1.1 将OpenSpec 1.6.0固定为项目开发依赖并初始化仓库内Codex工作流
- [x] 1.2 完成proposal、design和四个capability delta specs并通过OpenSpec校验
- [x] 1.3 先补充Agent、Memory、临时模式、删除和账号隔离的失败测试

## 2. Hermes-lite领域层

- [x] 2.1 实现版本化Skill Registry和Capability Router
- [x] 2.2 实现白名单Tool Registry、Policy Engine及current_time/memory_search/realtime_search工具
- [x] 2.3 实现最多3步Agent Orchestrator、工具结果回填和失败降级
- [x] 2.4 扩展Conversation Service以接入Agent、记忆引用、临时模式和Observer状态

## 3. 个人记忆领域层

- [x] 3.1 实现Observation/Profile Item类型、InMemory Repository和来源校验
- [x] 3.2 实现LLM Observer结构解析、confirmed/candidate分层和失败隔离
- [x] 3.3 实现相关召回、上下文预算、删除传播和owner隔离
- [x] 3.4 实现显式纠正的语义key、旧版本关闭和当前版本召回

## 4. CloudBase适配

- [x] 4.1 实现messages/observations/profile_items CloudBase repositories和所需API actions
- [x] 4.2 扩展CloudBase ModelAdapter以支持Agent信封、最终回复和Memory Observer
- [x] 4.3 添加实时搜索可替换适配器、环境配置说明和结构化不可用降级
- [x] 4.4 同步领域部署副本并扩展结构验证防止漂移
- [x] 4.5 增加fast/reasoner/multimodal/observer可选模型路由并保持单模型回退

## 5. 微信小程序体验

- [x] 5.1 扩展首页显示临时模式、记忆使用提示、工具状态和Observer失败状态
- [x] 5.2 在“我的”页增加记忆中心列表、来源展示和删除操作
- [x] 5.3 保证页面重载后从数据库恢复会话，并让失败气泡复用原requestId原位重试

## 6. 本地验证与文档

- [x] 6.1 运行领域、契约、隐私、结构和TypeScript全量验证
- [x] 6.2 用Fake Model跑通两轮记忆、一次工具调用、删除、临时模式和双账号脚本
- [x] 6.3 更新README和交接文档，区分代码POC、部署、微信预览和真机端测状态
- [x] 6.4 补齐工具参数Schema校验、零相关记忆过滤、末步工具拒绝及OpenSpec关键契约测试
- [x] 6.5 使用仿CloudBase SDK加载实际云函数入口，验证可信OPENID、消息落库、模型回复、记忆注入和账号隔离
- [ ] 6.6 运行OpenSpec validate和verify，完成实现任务后同步生效规格

## 7. CloudBase部署与端到端验收

现场状态（2026-07-18）：正式AppID预览包已成功生成，开发者工具编译为0个问题。项目管理员已提供一个确实存在的腾讯云CloudBase环境，因此无需重复购买19.9元套餐；但当前项目本地仍为`cloudProject: false`且微信CLI环境列表为空。直接指定该既有环境执行部署时，微信侧在查询函数和环境列表阶段均返回`system error`，证明当前小程序AppID/开发者工具账号尚未获得该环境的微信侧关联权限。从`conversations`目录执行“创建并部署”还会打开另一个名为“失物交接”的小程序控制台。为避免跨AppID误部署，未创建集合、未成功部署函数、未写入数据。下一步是在微信云开发的“账号绑定/关联已有腾讯云账号”流程中将当前MYALLY AppID关联到既有环境，再继续7.1；不得复用另一个小程序环境。

同日后续核验：本机忽略提交的项目配置曾回退到另一个旧AppID，导致GUI把`entries`和`conversations`上传到旧小程序环境；该环境未创建集合、未写入测试数据。恢复目标“失物交接”AppID后，CLI环境查询仍返回`system error`，其云开发控制台明确提示非个人主体未通过微信认证，且账号绑定仍停在“下一步”。所以7.1仍未开始。后续必须先由用户在目标AppID完成微信认证识别和腾讯云账号绑定，再同时核对应用名称、运行时AppID与CLI环境列表一致。

最新覆盖状态（2026-07-18）：用户已改用新建小程序并更新本机AppID。完整重开开发者工具后，CLI可列出该AppID的独立CloudBase环境，`entries`与`conversations`详情均为Active，旧的认证/绑定阻塞已解除。CLI更新部署仍被微信签名接口`41002`拒绝；GUI已对两个目录发起云端安装依赖更新，但尚未取得版本时间或远端副本一致性证据。因此7.1由“未开始”推进为“环境和函数可见，集合/索引及部署副本待核验”，仍不得勾选完成。

真实冒烟覆盖状态（2026-07-18）：新AppID预览发出的真实文字请求已经到达`conversations`，首个`messages`查询返回`-502005 / DATABASE_COLLECTION_NOT_EXIST`。因此函数入口连通已确认，但消息没有落库、LLM没有调用。代码已新增`SETUP_REQUIRED`安全响应，明确说明本次消息未保存，并以集成测试覆盖；安全版本已通过GUI再次上传，但无可审计的远端版本时间。7.1当前唯一确定的人工前置是按`cloudbase/schema.json`创建四个集合、拒绝客户端直连并建立索引；完成后立即重跑文字消息→落库→`hy3`回复冒烟。

最新执行状态（2026-07-18）：四个集合已在新环境创建并全部设为“所有用户不可读写”；按`cloudbase/schema.json`逐项创建并复核了11个业务索引，其中包含`messages(ownerId, requestId, role)`唯一索引。`conversations`远端更新时间已可审计，安全报错版本确认在线。再次真实发送后，`messages`出现一条用户记录且没有助手记录，证明前端→云函数→数据库已打通，失败区间收窄为模型调用/助手消息保存。移动端云开发控制台确认当前套餐为“个人版 / 开发期免费”，因此不得购买或点击“升级付费套餐”；按量付费和自动续费均关闭。AI模型页仍显示`hy3`开关关闭、免费额度为`-`并按资源点/Token计价，所以成长计划的环境权益已到账，但AI资源包到账状态仍需单独核验。为避免未经确认产生费用，没有启用模型或切换计费方式。7.1可以完成，7.2继续保持未完成；下一步只核验AI资源包，不购买19.9元套餐。

成长计划资源领取续测（2026-07-18）：成长计划页面已为当前环境显示“领取资源成功”，卡片明确包含云开发资源及混元10亿免费Token，AI资源包到账状态已确认。刷新AI模型页后，环境仍提示不是资源点计费；实际开启`hy3`返回“无法开启模型，切换后支持在套餐中抵扣”。因此7.2的唯一主链阻塞更新为“免费资源已到账，但环境尚未激活资源点抵扣模式”。后续不得购买19.9元套餐，不得开启按量付费或自动续费；只在确认成长计划资源可抵扣且无新增费用/订阅后完成模式切换，再验证真实模型回复。

资源点与真实LLM续测（2026-07-18，覆盖上一段当前结论）：个人版环境已无付费升级完成资源点模式切换，按量付费和自动续费保持关闭。`hy3`真实调用首次暴露Node.js 16.13缺少`structuredClone`；两个云函数入口加入兼容层并重新部署后，开发者工具重试已在小程序界面收到真实助手回复，文字发送→用户落库→模型→助手展示主链完成。临时诊断文案已撤回并部署安全通用错误响应。7.2仍保持未完成，因为Observer/Profile真实写入、记忆中心、第二轮召回、临时模式和白名单工具尚待逐项验收；7.3—7.4的真机与双账号验证也未完成。

真实评测续测（2026-07-18，覆盖上一段7.2边界）：专用评测页以固定中文用例真实调用CloudBase，临时模式成功获得回复并返回`memoryStatus=skipped`；普通回答和记忆建立用例在约3秒后返回`-504003 / FUNCTIONS_TIME_LIMIT_EXCEEDED`。根因是远端`conversations`仍保留CloudBase默认3秒执行超时，而该函数串行执行Agent回答和记忆观察器。部署清单已明确`conversations.timeoutSeconds=60`并加入测试约束；先更新远端函数配置，再重跑记忆建立、第二轮召回和工具用例。7.2继续保持未完成，但临时模式子项已真实通过。

超时修复与严格评测（2026-07-18，当前覆盖状态）：远端`conversations`执行超时已改为60秒。微信开发者工具严格评测通过普通建议、首轮Profile写入、第二轮记忆召回、临时模式和`current_time`白名单工具；“我的空间”显示真实已确认画像，7.2完成。重复运行固定记忆用例留下两条旧测试画像，未擅自删除；相同显式偏好去重已加入领域层和部署副本，单元测试通过并重新部署。7.3仍要求物理真机，7.4仍要求第二微信账号、删除传播和不可用降级。

部署准备：`cloudbase/schema.json`已固化四个集合、索引、拒绝客户端直连策略和两个云函数清单；`npm run cloud:check`可做无凭证检查，环境就绪后通过仅存在于本机进程的`MYALLY_CLOUDBASE_ENV_ID`运行`npm run cloud:deploy`。CLI不支持创建集合，需先在控制台按schema创建，且不得把长期云API密钥写入项目。

- [x] 7.1 在正式AppID绑定的测试环境创建集合和索引并部署conversations云函数
- [x] 7.2 微信开发者工具预览真实发送、数据库落库、模型回复、记忆中心和临时模式
- [ ] 7.3 真机完成两轮记忆影响回答和一次白名单工具调用
- [ ] 7.4 使用两个微信账号验证消息与记忆隔离、删除传播和模型/工具不可用降级
- [ ] 7.5 通过OpenSpec verify后同步主规格并归档change
