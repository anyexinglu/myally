# 任务

- [x] 1. 核对本地 AppID 配置和开发者工具项目身份。
- [x] 2. 修复记忆模块导出和对话默认模型漂移。
- [x] 3. 将语音识别改为原生录音 + 一句话识别，并复用 `send()`；移除主体受限插件。
- [x] 4. 增加模型与语音路由回归测试。
- [ ] 5. 通过全部自动测试、类型检查和 OpenSpec 严格校验。
- [x] 6. 在匹配环境部署 `conversations` 云函数并确认 60 秒超时。
- [x] 7.1 用户确认开通中国大陆语音识别，国内及跨境后付费均关闭。
- [x] 7.2 在匹配环境部署 `asr`，将远端超时设为 20 秒并经 CLI 核验为 Active，开发者工具重新编译为 0 错误。
- [ ] 7.3 真机验证麦克风授权、语音转写、文字复用发送和 AI 回复。
- [x] 8. 更新 README 与交接文档，记录已取得的部署证据和真机待测边界。

2026-07-19补充：`conversations/config.json`已声明`security.msgSecCheck`云调用权限并重新部署，开发者工具真实消息已再次取得`hy3`回复。模拟器录音可启动且0错误；由于工具录音格式不同于移动端，已补15秒上传超时和30秒ASR超时，异常时恢复文字输入。7.3仍以物理真机证据为准。

2026-07-19语音复测补充：修复`RecorderManager`停止/错误事件被错误赋值而未注册的问题，改用`onStop(callback)`/`onError(callback)`并清理监听器。开发者工具按住说话已真实转写“你好”并收到AI回复，模拟器端到端通过；物理真机仍需用户重新预览后确认。
